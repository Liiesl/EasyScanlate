[package]
name = "scanlateit-settings"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
confy = "2"
scanlateit-model = { path = "../model" }

[dev-dependencies]
tempfile = "3"
toml = "1"
